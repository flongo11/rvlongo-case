name: HTML to Text Extractor

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  extract-text:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          lfs: true
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc lynx
      
      - name: Find HTML files
        run: |
          echo "Searching for HTML files..."
          find . -name "*.html" -type f | head -20
      
      - name: Extract text from HTML files
        run: |
          mkdir -p extracted_text
          
          find . -path ./.git -prune -o -name "*.html" -type f -print | while read file; do
            echo "Processing: $file"
            
            base=$(basename "$file" .html)
            dir=$(dirname "$file" | sed 's|^\./||')
            
            mkdir -p "extracted_text/$dir"
            
            if pandoc "$file" -t plain -o "extracted_text/${dir}/${base}.txt" 2>/dev/null; then
              echo "✓ Extracted with pandoc: $file"
            elif lynx -dump -nolist "$file" > "extracted_text/${dir}/${base}.txt" 2>/dev/null; then
              echo "✓ Extracted with lynx: $file"
            else
              echo "✗ Failed: $file"
            fi
          done
          
          total=$(find extracted_text -name "*.txt" -type f | wc -l)
          echo "Total files extracted: $total"
      
      - name: Create README
        run: |
          {
            echo "# EXTRACTED TEXT FILES FOR GROK ANALYSIS"
            echo ""
            echo "**Purpose:** Plain text versions of all HTML evidence files for AI analysis."
            echo ""
            echo "**Generated:** $(date)"
            echo ""
            echo "**Total Files:** $(find extracted_text -name '*.txt' -type f | wc -l)"
            echo ""
            echo "## How to Use:"
            echo "1. Download EXTRACTED_TEXT_FOR_GROK.zip from repo root"
            echo "2. Extract all .txt files"
            echo "3. Upload to Grok at https://x.ai"
            echo ""
            echo "## Files Extracted:"
            find extracted_text -name "*.txt" -type f | sort
          } > extracted_text/README.md
      
      - name: Create downloadable ZIP
        run: |
          zip -r EXTRACTED_TEXT_FOR_GROK.zip extracted_text/
          
          size=$(du -h EXTRACTED_TEXT_FOR_GROK.zip | cut -f1)
          count=$(find extracted_text -name "*.txt" -type f | wc -l)
          
          echo "✅ ZIP created: EXTRACTED_TEXT_FOR_GROK.zip"
          echo "   Size: $size"
          echo "   Files: $count"
      
      - name: Commit results
        run: |
          git config user.name "GitHub Action"
          git config user.email "action@github.com"
          git add extracted_text/ EXTRACTED_TEXT_FOR_GROK.zip
          git diff --cached --quiet || git commit -m "Extract text from HTML for Grok analysis"
          git push || echo "Nothing to push"
      
      - name: Summary
        run: |
          echo "## 🎉 Extraction Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Files extracted:** $(find extracted_text -name '*.txt' -type f | wc -l)" >> $GITHUB_STEP_SUMMARY
          echo "**ZIP size:** $(du -h EXTRACTED_TEXT_FOR_GROK.zip | cut -f1)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Download EXTRACTED_TEXT_FOR_GROK.zip from repo root" >> $GITHUB_STEP_SUMMARY
